[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stock-game-pro"
version = "1.0.0"
description = "Educational global trading market simulator"
requires-python = ">=3.10"
